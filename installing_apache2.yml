---
- hosts: webserver
  become: true
  tasks:
    
    - name: Installing apache2 service
      apt:
         name: apache2
         state: present
         update_cache: yes
         
    - name: Starting the service
      service:
          name: apache2
          state: started
          enabled: yes
          
    #- name: Copying index file
     # copy: 
      #   src: index.html
       #  dest: /var/www/html/index.html
       
    - name: Downloading the website template
      uri: 
          url: https://www.tooplate.com/download/2121_wave_cafe.zip
          dest: /home/ubuntu/
          reomte_src: yes
             
  #  - name: unarchive
   #   unarchive: 
    #      src: /home/ubuntu/2121_wave_cafe.zip
     #     dest: /tmp/
      #    remote_src: yes
          
   # - name: copying files in apache2 location
    #  shell: cp -rf /tmp/2121_wave_cafe/* /var/www/html/ 
      
